import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    success: {
      main: '#90caf9',
    },
    primary: {
      main: '#06AAAA',
    },
    background: {
      default: '#1e1e1e',
    },
  },
})

export default theme
