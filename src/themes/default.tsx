import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'light',
    success: {
      main: '#90caf9',
    },
    primary: {
      main: '#06AAAA',
    },
    background: {
      default: '#f0f2f5',
    },
  },
})

export default theme
